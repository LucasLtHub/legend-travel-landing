# Carpetas generadas por build-tanda.py — no van al repo
dist-tanda1/
dist-tanda2/
dist-tanda3/

# Python
.venv/
__pycache__/
*.pyc

# Fotos originales del equipo (crudas, no publicadas — la web usa /equipo optimizadas)
Fotos de equipo/
